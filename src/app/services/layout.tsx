import type { Metadata } from 'next';

export const metadata: Metadata = { title: 'Bảng giá dịch vụ' };
export default function Layout({ children }: Readonly<{ children: React.ReactNode }>) { return children; }
